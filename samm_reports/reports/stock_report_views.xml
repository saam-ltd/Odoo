<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_report_picking" model="ir.actions.report">
            <field name="name">SSL - GRN</field>
            <field name="model">stock.picking</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">samm_reports.report_picking_operation_new</field>
            <field name="report_file">samm_reports.report_picking_operation_new</field>
            <!-- <field name="print_report_name">'Picking Operations - %s - %s' % (object.partner_id.name or '', object.name)</field> -->
            <field name="print_report_name">'GRN - %s' % (object.name or '')</field>
            <field name="binding_model_id" ref="stock.model_stock_picking"/>
            <field name="binding_type">report</field>
            <field name="paperformat_id" ref="samm_reports.paperformat_samm_delivery_report"/>
        </record>
    </data>
</odoo>