<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Partner View Inherit -->

    <record id="view_res_partner_form_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.view.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <field name="vat" position="after">
                <field name="brn"/>
                <field name="custom_salesperson_id"/> <!-- attrs="{'invisible': [('is_cus', '=',False)]}" -->
                <field name="gl_code"/>
                <field name="is_cus" invisible="1"/>
                <field name="customer_category_id" attrs="{'invisible': [('is_cus', '=',False)]}"/>
                <field name="cus_remarks" attrs="{'invisible': [('is_cus', '=',False)]}"/>
            </field>
        </field>
    </record>

    <!-- Over ridden base for adding an extra context -->

    <record id="account.res_partner_action_customer" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_customer': 1,'res_partner_search_mode': 'customer', 'default_is_company': True, 'default_customer_rank': 1,'default_is_cus': True}</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new customer in your address book
          </p><p>
            Odoo helps you easily track all activities related to a customer.
          </p>
        </field>
    </record>


    <record model="ir.ui.view" id="customer_category_form">
        <field name="name">Customer Category Form</field>
        <field name="model">customer.catgeory</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name" required='1'/>
                            <field name="company_id" invisible='1'/>
                        </group>
                        <group>
                        </group>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="customer_category_tree">
        <field name="name">Customer category Tree</field>
        <field name="model">customer.catgeory</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="company_id"/>
            </tree>
        </field>
    </record>

    <record id="action_customer_category" model="ir.actions.act_window">
        <field name="name">Customer Category</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">customer.catgeory</field>
        <!-- <field name="context">{'delete': 0}</field> -->
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Samm Customer -->

    <menuitem 
        id="customer_category"
        name="Customer Category"
        parent="sale.menu_sale_config"
        action="action_customer_category"
        sequence="50"/>
</odoo>
