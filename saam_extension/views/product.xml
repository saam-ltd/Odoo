<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- Product Template View Inherit -->

    <record id="view_product_template_form_inherit" model="ir.ui.view">
        <field name="name">product.templete.form.view.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <field name="barcode" position="after">
                <field name="scale_code"/>
            </field>
        </field>
    </record>

    <!-- <record model="ir.ui.view" id="view_stock_quant_tree_editable_avail_stock_report">
        <field name="name">stock.quant.tree.available.stock</field>
        <field name="model">stock.quant</field>
        <field eval="10" name="priority"/>
        <field name="arch" type="xml">
            <tree string="Available Stock Report" create="false" edit="false">
                <field name="display_name" string="Product Name"/>
                <field name="location_id" string="Location"/>
                <field name="inventory_quantity_auto_apply" string="Available Quantity"/>
                <field name="available_quantity" string="Reserved Quantity"/>
            </tree>
        </field>
    </record> -->

    <!-- <record id="action_available_stock_reserved_report" model="ir.actions.act_window">
        <field name="name">Available Stock  Reserved Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">stock.quant</field>
        <field name="view_id" ref="view_stock_quant_tree_editable_avail_stock_report"/>
        <field name="view_mode">tree</field>
        <field name="domain">[('location_id.usage','=', 'internal')]</field>
    </record> -->

    <!-- <menuitem 
        id="menu_available_stock_reserved_report"
        name="Available Stock Report"
        parent="sale.product_menu_catalog"
        action="action_available_stock_reserved_report"
        sequence="8"/> -->

</odoo>
